<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StarDeception - Sentinel</title>
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="manifest" href="/favicon/site.webmanifest">
    
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <div class="container">
            <h1>📡 StarDeception - Sentinel</h1>
            <p>Surveillance des serveurs de jeu en temps réel</p>
            
            <!-- Contrôles de configuration -->
            <div class="controls">
                <!-- Bouton toggle pour basculer entre serveurs et joueurs -->
                <div class="control-group toggle-group">
                    <label for="viewToggle">📊 Affichage:</label>
                    <div class="toggle-switch">
                        <input type="checkbox" id="viewToggle" class="toggle-input">
                        <label for="viewToggle" class="toggle-label">
                            <span class="toggle-inner">
                                <span class="toggle-option active">🖥️ Serveurs</span>
                                <span class="toggle-option">👥 Joueurs</span>
                            </span>
                            <span class="toggle-switch-slider"></span>
                        </label>
                    </div>
                </div>
                
                <!-- Bouton toggle pour basculer entre grille et liste -->
                <div class="control-group toggle-group">
                    <label for="layoutToggle">🔳 Layout:</label>
                    <div class="toggle-switch">
                        <input type="checkbox" id="layoutToggle" class="toggle-input">
                        <label for="layoutToggle" class="toggle-label">
                            <span class="toggle-inner">
                                <span class="toggle-option active">📱 Grille</span>
                                <span class="toggle-option">📋 Liste</span>
                            </span>
                            <span class="toggle-switch-slider"></span>
                        </label>
                    </div>
                </div>
                
                <div class="control-group">
                    <label for="refreshRate">⏱️ Refresh (sec):</label>
                    <input type="number" id="refreshRate" value="30" min="5" max="300" step="5">
                </div>
                <div class="control-group">
                    <label for="baseUrl">🌐 URL de Base:</label>
                    <input type="text" id="baseUrl" value="" placeholder="https://api.example.com">
                </div>
                <div class="control-group">
                    <button id="applySettings" class="apply-btn">✅ Appliquer</button>
                    <div id="countdownContainer" class="countdown-container" style="display: none;">
                        <span class="countdown-label">Prochain refresh dans:</span>
                        <div class="countdown-display">
                            <span id="countdownTime" class="countdown-time">30</span>
                            <span class="countdown-unit">s</span>
                        </div>
                        <div class="countdown-progress">
                            <div id="countdownBar" class="countdown-bar"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <main class="container">
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message">
                <p>⚠️ <%= error %></p>
            </div>
        <% } %>
        
        <% if (typeof message !== 'undefined' && message) { %>
            <div class="info-message">
                <p>ℹ️ <%= message %></p>
            </div>
        <% } %>

        <!-- Loader -->
        <div id="loader" class="loader-overlay" style="display: none;">
            <div class="loader-container">
                <div class="cube"><div class="cube__inner"></div></div>
                <div class="cube"><div class="cube__inner"></div></div>
                <div class="cube"><div class="cube__inner"></div></div>
            </div>
            <p class="loader-text">Chargement des données...</p>
        </div>

        <!-- Stats Serveurs -->
        <div class="stats servers-stats">
            <div class="stat-card">
                <h3><%= servers.length %></h3>
                <p>Serveurs Total</p>
            </div>
            <div class="stat-card">
                <h3><%= servers.filter(s => s.is_free === 1).length %></h3>
                <p>Serveurs Libres</p>
            </div>
            <div class="stat-card">
                <h3><%= servers.filter(s => s.is_free === 0).length %></h3>
                <p>Serveurs Occupés</p>
            </div>
        </div>

        <!-- Stats Joueurs -->
        <div class="stats players-stats" style="display: none;">
            <div class="stat-card">
                <h3 id="totalPlayers"><%= players.length %></h3>
                <p>Joueurs Total</p>
            </div>
            <div class="stat-card">
                <h3 id="uniqueServers">0</h3>
                <p>Serveurs Actifs</p>
            </div>
            <div class="stat-card">
                <h3 id="recentlyUpdated">0</h3>
                <p>Récemment Actifs</p>
            </div>
        </div>

        <!-- Grille des serveurs -->
        <div class="servers-grid">
            <% servers.forEach(server => { %>
                <div class="server-card <%= server.is_free === 1 ? 'free' : 'occupied' %>">
                    <div class="server-header">
                        <h3><%= server.name %></h3>
                        <span class="status-badge <%= server.is_free === 1 ? 'free' : 'occupied' %>">
                            <%= server.is_free === 1 ? '✅ Libre' : '🔴 Occupé' %>
                        </span>
                    </div>
                    
                    <div class="server-info">
                        <div class="info-row">
                            <span class="label">🌐 Adresse:</span>
                            <span class="value"><%= server.ip %>:<%= server.port %></span>
                        </div>
                        
                        <div class="info-row">
                            <span class="label">👥 Joueurs:</span>
                            <span class="value"><%= server.current_players %>/<%= server.max_players %></span>
                        </div>
                        
                        <% if (server.coordinate_x_start !== null) { %>
                            <div class="info-row">
                                <span class="label">📍 Coordonnées X:</span>
                                <span class="value"><%= server.coordinate_x_start %> → <%= server.coordinate_x_end %></span>
                            </div>
                            
                            <div class="info-row">
                                <span class="label">📍 Coordonnées Y:</span>
                                <span class="value"><%= server.coordinate_y_start %> → <%= server.coordinate_y_end %></span>
                            </div>
                            
                            <div class="info-row">
                                <span class="label">📍 Coordonnées Z:</span>
                                <span class="value"><%= server.coordinate_z_start %> → <%= server.coordinate_z_end %></span>
                            </div>
                        <% } else { %>
                            <div class="info-row">
                                <span class="label">📍 Coordonnées:</span>
                                <span class="value">Non définies</span>
                            </div>
                        <% } %>
                        
                        <div class="info-row">
                            <span class="label">🕐 Créé le:</span>
                            <span class="value"><%= new Date(server.created_at).toLocaleString('fr-FR') %></span>
                        </div>
                        
                        <div class="info-row">
                            <span class="label">🔄 Mis à jour:</span>
                            <span class="value"><%= new Date(server.updated_at).toLocaleString('fr-FR') %></span>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>

        <!-- Grille des joueurs -->
        <div class="players-grid" style="display: none;">
            <!-- Les joueurs seront ajoutés dynamiquement via JavaScript -->
        </div>

        <% if (servers.length === 0) { %>
            <div class="no-servers">
                <p>🔍 Aucun serveur trouvé</p>
            </div>
        <% } %>
    </main>

    <footer>
        <div class="container">
            <p>&copy; 2025 Star Deception Sentinel - Données mises à jour automatiquement</p>
            <p>
                <a href="https://github.com/StarDeception/SDO/tree/main/Sentinel" target="_blank" rel="noopener noreferrer" style="color: #00d4aa; text-decoration: none;">
                    📡 Voir sur GitHub
                </a>
            </p>
        </div>
    </footer>

    <script src="/js/script.js"></script>
</body>
</html>
